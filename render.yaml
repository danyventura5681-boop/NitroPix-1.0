services:
  - type: worker
    name: NitroPix
    env: python
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y libjpeg-dev zlib1g-dev libfreetype6-dev libopenjp2-7-dev libtiff5-dev
      pip install -r requirements.txt
    startCommand: python main.py