services:
  - type: worker
    name: nitropix
    env: python
    plan: free
    runtime: python3.11

    buildCommand: |
      chmod +x render-build.sh
      ./render-build.sh

    startCommand: python render_bot.py